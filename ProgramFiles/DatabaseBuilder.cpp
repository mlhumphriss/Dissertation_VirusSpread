#include "DatabaseBuilder.h"


DatabaseBuilder::initialiseDatabase(Person* worldPop[popDB]) {
	srand(seed);
	int	healthRisk;
	int	jobImportance;
	float rebeliousness;
	bool vaccinated;

	for (i = 0; i < popDB; ++i) {
		healthRisk = rand() % 0 2;
	}
}